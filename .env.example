# Copy to .env and fill in. The app also accepts the key typed into the sidebar.
OPENAI_API_KEY=sk-...
# Optional overrides
OPENAI_MODEL=gpt-4o
OPENAI_BASE_URL=
# Azure Retail Prices API needs no auth. Set to 0 to force offline fallback pricing.
LIVE_PRICING=1
